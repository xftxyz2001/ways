## 问题背景

不小心按到快捷键后，屏幕突然泛白、发灰，颜色显示不正常，可能是误开启了 HDR。如果屏幕完全变成黑白，也可能是开启了灰度颜色筛选器。

## 解决方案

### 关闭 HDR

按下 `Win + Alt + B`，切换 HDR 开关。如果刚才误开启了 HDR，再按一次即可关闭。切换过程中屏幕可能会短暂闪烁。

如果快捷键没有反应，可以手动关闭：

1. 打开 **设置 → 系统 → 屏幕**。
2. 如果连接了多个显示器，先选择出现问题的显示器。
3. 进入 **HDR**，关闭 **使用 HDR**。

### 关闭灰度颜色筛选器

如果屏幕完全没有颜色，只剩黑白灰，可以按下 `Win + Ctrl + C` 切换颜色筛选器。该快捷键需要事先在设置中启用。

也可以在 Windows 11 中打开 **设置 → 辅助功能 → 颜色筛选器**，关闭 **颜色筛选器**。Windows 10 对应的位置是 **设置 → 轻松使用 → 颜色滤镜**。

为避免再次误触，可以在同一页面关闭颜色筛选器的键盘快捷方式。
