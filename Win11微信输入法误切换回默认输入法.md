# Win11 微信输入法误切换回默认输入法

## 问题现象

使用微信输入法时，输入法偶尔会切换成微软拼音或其他输入法。通常不是微信输入法自动退出，而是误触了 Windows 的输入法切换快捷键，或者 Windows 为不同应用窗口保存了不同的输入法。

常见触发键包括：

- `Win + 空格`：依次切换已安装的输入法或键盘布局。
- `Alt + Shift`：切换输入语言或键盘布局，是否生效取决于系统设置。
- `Ctrl + Shift`：可能被配置为输入法切换键。
- `Ctrl + 空格`：可能切换输入法或输入模式，具体行为取决于系统及输入法设置。
- 单按 `Shift`：微信输入法中通常只切换中英文状态，并不等于切换到微软拼音。

## 推荐设置

### 1. 将微信输入法设为默认输入法

打开：

> 设置 → 时间和语言 → 输入 → 高级键盘设置

在“替代默认输入法”中选择“微信输入法”。

### 2. 关闭不同窗口使用不同输入法

仍在“高级键盘设置”页面，取消勾选：

> 允许我为每个应用窗口使用不同的输入法

开启该选项时，Windows 会记住各个窗口上次使用的输入法。切换窗口后恢复成微软拼音，容易让人误以为输入法被快捷键切换了。

### 3. 取消输入语言切换快捷键

在“高级键盘设置”中打开：

> 输入语言热键 → 高级键设置

依次检查以下项目：

- 在输入语言之间
- 在键盘布局之间

选择项目后点击“更改按键顺序”，将快捷键设为“未分配”。这样可以避免误触 `Alt + Shift` 或 `Ctrl + Shift`。

> `Win + 空格` 是 Windows 的系统级切换快捷键，通常不能在上述页面直接关闭。减少可切换的输入法是更可靠的规避方式。

### 4. 删除不使用的微软拼音

如果平时只使用微信输入法，可以打开：

> 设置 → 时间和语言 → 语言和区域 → 中文（简体，中国）→ 语言选项 → 键盘

找到“微软拼音”，点击右侧菜单并选择“删除”。

删除前确认微信输入法已经安装且能够正常输入。只留下一个日常使用的中文输入法后，即使误按 `Win + 空格`，也不会在多个中文输入法之间反复切换。

### 5. 检查微信输入法自己的快捷键

打开微信输入法设置，进入“按键”或“快捷键”相关页面，检查以下按键：

- 中英文切换
- 输入法切换
- `Shift`
- `Ctrl + 空格`
- `Ctrl + Shift`

如果某项容易误触，可将其关闭或改成不常用的组合键。不同版本的微信输入法设置名称可能略有不同。

## 建议采用的最终组合

一般情况下，完成下面三项即可解决：

1. 将微信输入法设为默认输入法。
2. 关闭“允许我为每个应用窗口使用不同的输入法”。
3. 不使用微软拼音时将其删除。

如果需要保留微软拼音，则不要删除，只取消“输入语言”和“键盘布局”的切换快捷键，并尽量避免误按 `Win + 空格`。

## 如何判断是哪种情况

- 按键后屏幕右侧出现输入法列表：通常是触发了 `Win + 空格`。
- 切换到另一个软件后输入法发生变化：通常是开启了“允许我为每个应用窗口使用不同的输入法”。
- 仍显示微信输入法，但只能输入英文：通常只是按下 `Shift` 切换到了英文模式。
- 任务栏输入法名称变成“微软拼音”：说明确实切换了输入法。

## 参考资料

- [微软：在 Windows 中管理语言和键盘/输入布局设置](https://support.microsoft.com/zh-cn/windows/hardware/input-devices/manage-the-language-and-keyboard-input-layout-settings-in-windows)
- [微软：Windows 的键盘快捷方式](https://support.microsoft.com/zh-cn/windows/windows-%E7%9A%84%E9%94%AE%E7%9B%98%E5%BF%AB%E6%8D%B7%E6%96%B9%E5%BC%8F-dcc61a57-8ff0-cffe-9796-cb9706c75eec)
